# 循迹斑马线对齐 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让小车低速接近斑马线停车横条，屏蔽斑马线条纹对巡线的干扰，停车原地摆正后再通过。

**Architecture:** 保留 `line_cy.py` 的现有视觉和路口通过结构，在检测结果中补充旋转多边形、横条角度和底部位置。正常处理改为 `FOLLOW_LINE -> APPROACH_CROSSWALK -> ALIGN_STOPLINE -> MANEUVER`，巡线始终使用已清除斑马线候选区域的二值图。

**Tech Stack:** Python 3、OpenCV、NumPy、ROS Melodic `rospy`、`unittest`

## Global Constraints

- WSL 编辑路径为 `/home/zcy/eaibot/robocom_ws/src`，实车同步路径为 `/home/eaibot/robocom_ws/src`。
- 默认继续保持 `DRY_RUN=True`、`DETECT_ONLY=True`。
- 对齐失败或横条持续丢失时必须停车，不能盲目进入路口。
- 不修改 `line_test_cy.py` 和三个 `beiyong*.py` 备用文件。

---

### Task 1: 横条几何信息和斑马线屏蔽

**Files:**
- Create: `/home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`
- Modify: `/home/zcy/eaibot/robocom_ws/src/line_cy.py:137-655`

**Interfaces:**
- Produces: `long_edge_angle_deg(points) -> float`
- Produces: 检测结果字段 `candidate`、`stop_polygon`、`stop_angle_deg`、`stop_bottom_y`、`stripe_polygons`
- Produces: `LaneFollower.suppress_crosswalk_regions(binary, stop_result) -> np.ndarray`

- [x] **Step 1: 写失败测试**

测试用旋转矩形验证水平角为 0、斜线角度符号稳定，并验证屏蔽后横条和竖条像素清零、真实边线像素保留。

- [x] **Step 2: 运行测试确认失败**

Run: `python3 /home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`

Expected: 因 `long_edge_angle_deg` 或旋转多边形字段尚不存在而失败。

- [x] **Step 3: 最小实现检测几何字段**

在轮廓检测阶段保存 `cv2.boxPoints(rect)`，使用最长边计算相对水平线角度，并让评分结果同时携带普通框和旋转多边形。

- [x] **Step 4: 最小实现多边形屏蔽**

`suppress_crosswalk_regions()` 优先用 `cv2.fillConvexPoly()` 清除旋转多边形，旧检测结果没有多边形时回退到普通矩形。

- [x] **Step 5: 运行测试确认通过**

Run: `python3 /home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`

Expected: 几何和屏蔽测试全部 PASS。

### Task 2: 接近与摆正状态控制

**Files:**
- Modify: `/home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`
- Modify: `/home/zcy/eaibot/robocom_ws/src/line_cy.py:20-134,658-1060`

**Interfaces:**
- Produces: `alignment_angular(angle_deg, kp, min_angular, max_angular, direction_sign) -> float`
- Produces: 状态 `APPROACH_CROSSWALK`、`ALIGN_STOPLINE`
- Consumes: Task 1 的检测几何字段和清理图。

- [x] **Step 1: 写失败测试**

验证角度误差为 0 时角速度为 0；正负角度产生相反角速度；输出满足最小有效角速度和最大限幅。

- [x] **Step 2: 运行测试确认失败**

Run: `python3 /home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`

Expected: 因 `alignment_angular` 尚不存在而失败。

- [x] **Step 3: 实现参数和状态机**

增加低速接近、底部触发比例、对齐比例、角速度限制、容差、稳定帧、超时、方向符号和丢失帧参数。候选稳定后低速靠近；横条到底后线速度归零并按角度旋转；稳定达标后调用现有 `run_maneuver()`。

- [x] **Step 4: 增加失效保护**

接近或对齐阶段持续丢失横条、以及对齐超时时，都进入 `CROSSWALK_WAIT` 并保持停车，不调用 `run_maneuver()`。

- [x] **Step 5: 运行测试确认通过**

Run: `python3 /home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`

Expected: 全部测试 PASS。

### Task 3: 调试显示、操作文档和最终验证

**Files:**
- Modify: `/home/zcy/eaibot/robocom_ws/src/line_cy.py:1074-1127`
- Modify: `/home/zcy/eaibot/zcy/循迹操作.md`

**Interfaces:**
- Consumes: Task 1/2 的状态和检测字段。
- Produces: 处理图中的横条角度、底部比例、对齐稳定帧，以及实车低速测试命令。

- [x] **Step 1: 更新调试叠加**

画出旋转横条轮廓，顶部显示 `angle`、`bottom` 和 `align`，确保斑马线黄色框不再进入蓝色/青色边线结果。

- [x] **Step 2: 更新协作文档**

补充新状态、参数说明、默认安全测试命令和实车方向反转参数 `_align_angular_sign:=-1.0`。

- [x] **Step 3: 完整验证**

Run: `python3 -m py_compile /home/zcy/eaibot/robocom_ws/src/line_cy.py /home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`

Run: `python3 /home/zcy/eaibot/robocom_ws/src/test_line_cy_crosswalk.py`

Expected: 语法检查成功且全部离线测试 PASS。

- [x] **Step 4: 实车同步说明**

同步 `/home/zcy/eaibot/robocom_ws/src/line_cy.py` 到 `/home/eaibot/robocom_ws/src/line_cy.py`，先以 `_dry_run:=true _detect_only:=true` 检查角度，再低速启用底盘。
